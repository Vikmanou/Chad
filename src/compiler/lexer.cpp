#include "chad/compiler/lexer.h"

#include <string>

namespace chad {

TokenList lex(const std::string& source) {
    TokenList tokens;

    int idx = 0;
    while (idx < source.size()) {
        ++i
    }

    return tokens;
}

}